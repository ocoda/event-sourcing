{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"benchmark:build": {
			"dependsOn": ["^build"]
		},
		"benchmark": {
			"dependsOn": ["benchmark:build"],
			"cache": false
		}
	}
}
